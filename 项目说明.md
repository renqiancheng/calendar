# 项目说明

中国农历 / 公历换算库，基于 [mumuy/calendar](https://github.com/mumuy/calendar) 维护。算法遵循国标 GB/T 33661—2017，支持 1900～2100 年范围内的阴阳历互转，以及干支、生肖、节气、节日等信息查询。

NPM 包名：`@rqiancheng/calendar`

---

## 目录结构

```
calendar/
├── src/                        # 源码
│   ├── calendar.js             # 库入口，对外暴露 API
│   ├── widget-calendar.js      # 万年历 Web Component
│   ├── style/
│   │   └── widget.css          # 组件样式（构建时内联）
│   └── module/
│       ├── date.js             # 聚合层：组装完整日期信息
│       ├── solar.js            # 公历 ↔ 时间戳
│       ├── lunar.js            # 农历 ↔ 时间戳（含闰月）
│       ├── ganzhi.js           # 干支纪年 / 月 / 日
│       ├── animal.js           # 生肖
│       ├── zodiac.js           # 星座
│       ├── term.js             # 二十四节气
│       ├── festival.js         # 节日（公历 / 农历 / 节气相关）
│       ├── tool.js             # 工具函数
│       └── config/
│           ├── base.js         # 年份范围等基础常量
│           └── holiday.js      # 法定节假日「休 / 班」静态配置
├── dist/                       # 构建产物（NPM 发布内容）
│   ├── calendar.min.js         # UMD / CommonJS
│   ├── calendar.min.mjs        # ESM
│   └── widget-calendar.min.js  # Web Component（UMD）
├── index.html                  # 演示页
├── static/                     # 演示页静态资源
├── rollup.config.mjs           # Rollup 构建配置
├── babel.config.js
├── package.json
└── readme.md                   # 对外 API 文档
```

---

## 环境要求

- Node.js >= 20
- npm >= 8

---

## 调试与运行

### 1. 安装依赖

```bash
npm install
```

### 2. 构建

```bash
# 一次性构建
npm run build

# 监听源码变更，自动重新构建
npm run watch
```

构建输出到 `dist/`，`index.html` 和 NPM 包均依赖该目录。

### 3. 本地预览演示页

项目无内置 dev server，需用静态服务器打开根目录 `index.html`。

**方式 A：npx serve（推荐）**

```bash
npm run build
npx serve .
```

浏览器访问终端提示的地址（一般为 `http://localhost:3000`）。

**方式 B：Python**

```bash
npm run build
python -m http.server 8080
```

访问 `http://localhost:8080`。

**方式 C：VS Code / Cursor**

安装 Live Server 等扩展，对 `index.html` 右键「Open with Live Server」。

> 演示页通过 `<script src="dist/widget-calendar.min.js">` 加载构建产物。修改 `src/` 后须重新 `build`，或另开终端运行 `npm run watch`。

### 4. Node 中快速验证

```bash
npm run build
node -e "const c=require('./dist/calendar.min.js'); console.log(c.getToday())"
```

### 5. 发布 NPM

```bash
npm publish --access public
```

`prepublishOnly` 会在发布前自动执行 `npm run build`。scoped 包每次发布建议带 `--access public`。

---

## 项目逻辑

### 整体流程

```
公历/农历/时间戳 输入
        ↓
  solar.js / lunar.js   →  统一转为本地时区午夜时间戳
        ↓
     date.js (getDateInfo)  →  聚合各模块计算结果
        ↓
  返回完整日期对象（阴阳历、干支、节气、节日等）
```

### 入口 API（`src/calendar.js`）

| 方法 | 说明 |
|------|------|
| `getDateBySolar(sYear, sMonth, sDay)` | 公历 → 完整日期信息 |
| `getDateByLunar(lYear, lMonth, lDay, isLeap)` | 农历 → 完整日期信息，`isLeap` 表示闰月 |
| `getDateByTimestamp(timestamp)` | 时间戳 → 完整日期信息 |
| `getToday()` | 取本地时区「今天」 |

无效日期返回 `null`。

### 核心模块职责

| 模块 | 职责 |
|------|------|
| `solar.js` | 公历与时间戳互转；星期计算 |
| `lunar.js` | 农历数据表（1900～2100 压缩编码）、闰月、大小月、阴阳历互转 |
| `date.js` | 调用各子模块，合并为统一返回结构 |
| `ganzhi.js` | 干支年 / 月 / 日（正月初一起算） |
| `animal.js` | 生肖（与农历新年同步） |
| `zodiac.js` | 西方星座 |
| `term.js` | 二十四节气 |
| `festival.js` | 公历节日、农历节日、节气相关节日 |
| `config/holiday.js` | 国务院放假安排的「休 / 班」标记（静态维护） |

### 时区处理（本 fork 重要改动）

公历时间戳统一使用**本地时区午夜**：

```js
new Date(sYear, sMonth - 1, sDay, 0, 0, 0, 0).getTime()
```

`getToday()`、`getDateBySolar()`、`widget-calendar` 因此保持一致。服务端若运行在 UTC 容器，应使用客户端传入的年月日，而非直接调用 `getToday()`。

### 万年历组件（`widget-calendar.js`）

- 基于 Custom Elements + Shadow DOM
- 注册标签名：`<widget-calendar>`
- 属性：`date`（默认今天）、`mode`
- 事件：`onInit`、`onChange`、`onSelect`
- 样式通过 `import ... with { type: 'css' }` 在构建时打包进 JS

#### 布局与响应式缩放

组件使用 CSS Container Queries（`:host { container-type: inline-size }`）按容器宽度切换布局：

| 容器宽度 | 布局 |
|----------|------|
| ≤ 480px | 上下栏（信息区在上、日历在下） |
| > 480px | 左右栏（信息区在左、日历在右） |

尺寸通过 `--base-size` / `--base-size-mobile` 等比缩放（基于 `100cqw` 与设计宽度之比，上限 1.1px）。容器变窄时会继续缩小，避免内容按固定 480px 排版后被 `:host { overflow: hidden }` 裁切（如周日列）。

#### 可配置 CSS 变量

以下变量可在 `<widget-calendar>` 元素上设置（会穿透 Shadow DOM）：

| 变量 | 默认值 | 作用 |
|------|--------|------|
| `--calendar-width` | `100%` | 组件宽度 |
| `--calendar-min-width` | `0` | 最小宽度；窄容器（如 Popover ~288px）默认不撑破父级，可按需设回 `360px` |
| `--calendar-max-width` | `600px` | 最大宽度 |
| `--calendar-design-width` | `600` | 桌面布局缩放基准（`100cqw` 除数） |
| `--calendar-mobile-design-width` | `480` | 窄屏布局缩放基准 |
| `--calendar-padding` | 随 `--base-size*` 计算 | 内边距 |
| `--primary-color` / `--secondary-color` / `--weekend-color` | 见 `widget.css` | 主题色 |

```html
<!-- Popover：固定 400px，保持上下栏 -->
<widget-calendar
  date="2026-6-18"
  style="display:block; width:400px; --calendar-min-width:400px;"
></widget-calendar>

<!-- 极窄容器：占满父级，等比缩小 -->
<widget-calendar style="display:block; width:100%;"></widget-calendar>
```

对外 API 与事件示例见 [readme.md](./readme.md#网页组件)。

### 构建（Rollup）

两个入口分别打包：

1. `src/calendar.js` → `calendar.min.js`（UMD）+ `calendar.min.mjs`（ESM）
2. `src/widget-calendar.js` → `widget-calendar.min.js`（UMD）

经 Babel 转译、Terser 压缩，banner 读取 `package.json` 中的包名与版本。

---

## 引用方式

### NPM 安装

```bash
npm install @rqiancheng/calendar
```

### ESM（现代打包工具 / 浏览器 module）

```js
import calendar from '@rqiancheng/calendar';

calendar.getDateBySolar(2026, 6, 17);
calendar.getToday();
```

### CommonJS / Node

```js
const calendar = require('@rqiancheng/calendar');

console.log(calendar.getToday());
```

### 子路径：Web Component

```js
import '@rqiancheng/calendar/widget-calendar';
```

```html
<widget-calendar date="2026-06-17"></widget-calendar>
```

```js
document.querySelector('widget-calendar').addEventListener('onSelect', (e) => {
  console.log(e.detail);
});
```

### 浏览器直接引用（无打包）

```html
<script src="node_modules/@rqiancheng/calendar/dist/calendar.min.js"></script>
<script>
  console.log(calendar.getToday());
</script>
```

```html
<script src="node_modules/@rqiancheng/calendar/dist/widget-calendar.min.js"></script>
<widget-calendar></widget-calendar>
```

### 本地开发引用（未发布时）

```js
import calendar from './dist/calendar.min.mjs';
```

---

## 返回数据结构示例

```js
{
  date: '2026-06-17',
  sYear: 2026, sMonth: 6, sDay: 17,
  lYear: 2026, lMonth: 5, lDay: 3,
  isLeap: false,
  lMonthZH: '五月',
  lDayZH: '初三',
  gzYearZH: '丙午',
  gzMonthZH: '甲午',
  gzDayZH: '壬戌',
  week: 3,
  weekZH: '星期三',
  animal: '马',
  term: '',
  zodiac: '双子座',
  festival: ''
}
```

---

## 常见维护场景

| 场景 | 操作 |
|------|------|
| 修改换算逻辑 | 编辑 `src/module/`，`npm run build` |
| 更新法定节假日「休 / 班」 | 编辑 `src/module/config/holiday.js` 的 `scheduleMap`，重新构建 |
| 调整组件 UI / 响应式布局 | 编辑 `src/widget-calendar.js` 或 `src/style/widget.css`（缩放变量见上文「布局与响应式缩放」） |
| 发新版本 | `npm version patch` → `npm publish --access public` |

---

## 相关链接

- 仓库：https://github.com/renqiancheng/calendar
- NPM：https://www.npmjs.com/package/@rqiancheng/calendar
- 上游项目：https://github.com/mumuy/calendar
